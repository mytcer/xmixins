/**
 * x-transparent
 * @link 
 */
x-transparent($bgcolor, $opacity) {
	$opacityIE = $opacity * 100;
	filter unquote('alpha(opacity=' + $opacityIE + ')');
	background-color: $bgcolor;
	background-color: rgba(red($bgcolor), green($bgcolor), blue($bgcolor), $opacity);	
}